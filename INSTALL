JzLauncher 安装说明
===================

1. 系统要求
-----------

- Linux 桌面环境
- Rust 1.85 或更高版本（项目使用 Rust 2024 Edition）
- GTK 4.22 或更高版本
- libadwaita 1.9 或更高版本
- pkg-config 和 C/C++ 基础构建工具

Debian/Ubuntu 可使用以下命令安装系统构建依赖：

    sudo apt update
    sudo apt install build-essential pkg-config libgtk-4-dev libadwaita-1-dev

如果系统软件源提供的 GTK 或 libadwaita 版本低于上述要求，需要升级到
兼容版本后再进行构建。

Rust 工具链推荐通过 rustup 安装：

    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
    rustup toolchain install stable
    rustup default stable

2. 从源码构建
-------------

在项目根目录执行：

    cargo build --release -p launcher-gui

构建完成后的可执行文件位于：

    target/release/jz-launcher

可以直接运行：

    ./target/release/jz-launcher --toggle

3. 构建并安装 deb 软件包
------------------------

首先安装 cargo-deb：

    cargo install cargo-deb

然后在项目根目录执行：

    cargo build --release -p launcher-gui
    cargo deb -p launcher-gui

生成的软件包位于 target/debian 目录。可使用以下命令安装：

    sudo apt install ./target/debian/jz-launcher_*.deb

卸载软件包：

    sudo apt remove jz-launcher

4. 全局快捷键
-------------

GTK 应用内快捷键无法在窗口隐藏或其他应用获得焦点时唤起 JzLauncher。
请在桌面环境的“设置 -> 键盘 -> 自定义快捷键”中添加快捷键，并将命令设置为：

    /usr/bin/jz-launcher --toggle

从源码直接运行且未安装 deb 软件包时，请将命令中的可执行文件替换为
target/release/jz-launcher 的绝对路径。
